import fetchStargazers from './fetch-stargazers'

describe('fetchStargazers', () => {
  it('exists', () => {
    expect(fetchStargazers).toBeDefined()
  })

  it('runs a test', () => {
    expect(1).toEqual(1)
  })
})
